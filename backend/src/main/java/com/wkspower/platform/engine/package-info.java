/**
 * Embedded BPMN engine adapter — the ONLY package allowed to import the engine SDK ({@code
 * org.cibseven.*} in this distribution; the package boundary keeps it swappable). Enforced by
 * {@code ArchitectureTest}.
 *
 * <p>Empty in Story 1.1; populated when the engine is activated in Story 2.2.
 */
package com.wkspower.platform.engine;
