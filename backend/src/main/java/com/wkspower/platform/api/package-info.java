/**
 * REST API surface — controllers, DTOs, mappers, and the global exception handler. Controllers are
 * thin: validate input, delegate to a domain service, map the result.
 */
package com.wkspower.platform.api;
