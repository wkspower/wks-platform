/**
 * Case-type configuration domain — static metadata shaping how Case/Task aggregates behave at
 * runtime. Kept separate from {@code domain.model} (runtime aggregates) so ArchUnit rules can
 * target "config types" independently from "case lifecycle types" in later stories.
 */
package com.wkspower.platform.domain.config;
