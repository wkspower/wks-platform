/**
 * Domain layer — pure Java, zero framework dependencies.
 *
 * <p>ArchUnit enforces (see {@code ArchitectureTest}): no imports from Spring, JPA, or the embedded
 * BPMN engine SDK are permitted in this package or its subpackages.
 */
package com.wkspower.platform.domain;
