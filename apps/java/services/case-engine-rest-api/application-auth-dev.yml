# Concern fragment — Authentication: embedded dev-token issuer (no Keycloak).
# The resource server validates dev tokens via the in-process /dev-auth JWKS, so
# the unified issuer URL points back at this app.
wks.auth.mode: dev-token
wks.auth.issuer-uri: ${KEYCLOAK_URL:http://localhost:8081/dev-auth}
